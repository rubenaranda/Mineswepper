{
  "scripts": {
    "test:e2e": "cucumber-js --require cucumber.conf.js --require tests/steps/**/*.steps.js --format @cucumber/pretty-formatter --format html:tests/reports/cucumber-report.html --parallel 1"
  },
  "devDependencies": {
    "@cucumber/cucumber": "^8.5.3",
    "playwright": "^1.25.2"
  }
}
